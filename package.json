{
  "name": "react_e-commerce",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "nodemon --watch backend --exec babel-node backend/server.js",
    "build": "rimraf dist && babel backend -d dist && cd e-commerce && npm install && npm run build",
    "heroku-postbuild": "npm run build && cd e-commerce && npm install && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.1",
    "dotenv": "^8.2.0",
    "jsonwebtoken": "^9.0.0",
    "express": "^4.18.2",
    "mongoose": "^5.13.20"
  },
  "devDependencies": {
    "@babel/cli": "^7.10.1",
    "@babel/core": "^7.10.1",
    "@babel/node": "^7.10.1",
    "@babel/preset-env": "^7.10.1",
    "babel-cli": "^6.26.0",
    "mkdirp": "^1.0.4",
    "nodemon": "^2.0.22",
    "rimraf": "^3.0.2"
  },
  "engines": {
    "node": "12.4.0",
    "npm": "6.9.0"
  }
}
